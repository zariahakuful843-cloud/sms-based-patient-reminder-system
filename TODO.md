# TODO - Dashboard UI Redesign (UI only)

- [ ] Update `app/(dashboard)/dashboard/page.tsx` to add modern hospital dashboard header (date, greeting, user name/role, notification icon) and redesign layout/cards/sections per requirements.
- [ ] Update `components/layout/Sidebar.tsx` branding + improve icon alignment and active styling (menu items unchanged).
- [ ] Optionally adjust `app/globals.css` only for dashboard palette/spacing helpers (no backend).
- [ ] Run dev/test: `npm run dev`, verify responsive layout and that all existing data render correctly.

